#include<stdio.h>
int main(){
    int limit,i,sum=0;
    int a[i];
    scanf("%d",&limit);
    for(i=0;i<limit;i++){
    scanf("%d ",&a[i]);}
    for(i=0;i<limit;i++){
            sum+=a[i];
        }
printf("The sum of the first %d numbers is %d.",limit,sum);
printf("\nThe average of the first %d numbers is %d.",limit,(sum/limit));}
