# include<stdio.h>
int len(int);
int armstrong(int);
int main(){
int num;
scanf("%d",&num);
int copy=num;
int power=len(num);
int val=armstrong(num);
if(val==num)
printf("Armstrong no");
else
printf("Not an armstrong number");}
int armstrong(num){if(num!=0)
int k=n%10;
int ans+=pow(k,len());
return armstrong*;}
